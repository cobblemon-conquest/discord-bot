info:
  name: test
  type: http
  seq: 3

http:
  method: POST
  url: "{{host}}/exceptions/discord/retry-dead-letter/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
